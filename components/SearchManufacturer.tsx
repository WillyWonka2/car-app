import { SearchManufacturerProps } from "@/types";

const SearchManufacturer = ({manufacture, setManufacture}: SearchManufacturerProps) => {
    return (
        <div>

        </div>
    );
};

export default SearchManufacturer;
